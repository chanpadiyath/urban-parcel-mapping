# Urban Parcel Mapping — environment template
# Copy to .env and fill in as needed. No secrets are required for the P0 MVP.

# Optional: override the base map raster tile URL template.
# Default (used when unset): https://tile.openstreetmap.org/{z}/{x}/{y}.png
# VITE_BASEMAP_TILE_URL=
